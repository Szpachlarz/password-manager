<UserControl x:Class="password_manager.WPF.Views.DetailsPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:password_manager.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"  Text="Tytuł wpisu" FontSize="26"/>
            <Button Height="50px" Margin='583,20,115,20' Content="Usuń wpis" />
            <Button Height="50px" Margin="702,20,10,20" Content="Powrót" Grid.ColumnSpan="3"/>
            <Button Height="50px" Margin="460,20,252,20" Content="Edytuj wpis"/>

        </Grid>

        <Grid Grid.Row="1">

        </Grid>

    </Grid>
</UserControl>
